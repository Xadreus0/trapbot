services:
  trapbot:
    build: .
    container_name: trapbot
    restart: unless-stopped
    environment:
      DISCORD_TOKEN: ${DISCORD_TOKEN}

      
      SWEEP_INTERVAL_SECONDS: "15"
    volumes:
      - ./data:/app/data
